spring.application.name=backend
spring.main.allow-bean-definition-overriding=true
logging.level.org.springframework.data.mongodb=DEBUG

# Server
server.port=8080

# MongoDB
spring.mongodb.database=p2p
spring.mongodb.uri=mongodb+srv://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_CLUSTER}/${MONGO_DB}


